# Copy this file to .env and fill in your values.
# NEVER commit the .env file -- it contains secrets.

# Overwatch test credentials
OW_USERNAME=your-email@example.com
OW_PASSWORD=your-password-here

# Overwatch base URL (no trailing slash)
OW_BASE_URL=https://test.auth.overwatch.luminize.com
